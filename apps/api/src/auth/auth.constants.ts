export const AUTH_COOKIE = 'lane_token';

/** Cookie max-age in seconds (7 days). */
export const COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 7;

/** JWT expiry as a string recognised by jsonwebtoken. */
export const JWT_EXPIRY = '7d';
